/*

#include <Rcpp.h>
#import <math.h>
#include "functions.h"
using namespace Rcpp;




// [[Rcpp::export]]
CharacterVector cFromLines (const std::string str) {

	std::string out;

	for (size_t ith = 0; ith < str.size(); ++ith) {
		if (str[ith] != ' ') out += str[ith];
	}

	return out;

}
*/
